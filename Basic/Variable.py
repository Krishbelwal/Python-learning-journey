# variables- variables like a container, it name of  memory location where we strore the values.
# imp points-
# variables are always case sensitive in c++.
# variables must be starts with either (a,z) or (A,Z) or underscore.
# we can use digits but will alwayes be start with letter
# we can't give space between variable.
a = "Hello world"
print(a ,"krish")
